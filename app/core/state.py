# Stores the configuration (loaded from .config/lyrebird/config.toml)
config = None

# Stores the persisted session (loaded from .config/lyrebird/session.toml)
session = None

# Stores all of the loaded presets (loaded from presets.toml)
loaded_presets = []

# Stores the current preset
current_preset = None

audio = None
